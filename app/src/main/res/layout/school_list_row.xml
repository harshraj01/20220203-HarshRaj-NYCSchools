<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View" />
        <variable
            name="schoolListModel"
            type="com.example.a20220203_harshraj_nycschools.model.SchoolListModel"/>

        <variable
            name="schoolListViewModel"
            type="com.example.a20220203_harshraj_nycschools.viewmodel.SchoolListViewModel"/>
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <RelativeLayout
            android:id="@+id/view_foreground"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:padding="10dp"
            android:clickable="true"
            android:onClick="@{(view) -> schoolListViewModel.onItemClick(view)}"
            >

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/fullname"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:textSize="12sp"
                    android:textColor="@color/teal_200"
                    android:textAllCaps="true"
                    android:lineSpacingExtra="4dp"
                    android:text="@{schoolListModel.fullname}"

                    />
            </LinearLayout>
        </RelativeLayout>

    </FrameLayout>

</layout>
